const arr = ['nima', 'parsa', 'omid', 'ali'];
const secondArr = ['shiva', 'zohre'];

const newArr = arr.pop();
console.log('Deleted the last name from array and returns it: ', newArr);
console.log('the modified array: ', arr);

const newSecArr = secondArr.pop();
console.log('this method only removes the last element: ', secondArr);
